# ExternalDNS: Helm Values (DEV)
# Route53 dev.mgz-g2-u3.shop 레코드 자동 관리

provider: aws

aws:
  region: ap-northeast-1
  zoneType: public

# IRSA 역할 ARN
serviceAccount:
  create: true
  name: external-dns
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::827913617839:role/jung-kyeol-dev-external-dns-role"

# 도메인 필터
domainFilters:
  - mgz-g2-u3.shop

# TXT 레코드 소유자 ID (환경별 고유)
txtOwnerId: jung-kyeol-dev

# 정책: sync = 생성/업데이트/삭제 모두 관리
policy: sync

# 소스: Ingress, Service
sources:
  - ingress
  - service

# 리소스 설정 (DEV는 최소)
resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi

# 로깅
logLevel: info
logFormat: text
